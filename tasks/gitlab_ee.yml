---

- name: Apt | Add gitlab-ee key
  ansible.builtin.get_url:
    url: https://packages.gitlab.com/gitlab/gitlab-ee/gpgkey
    dest: /etc/apt/keyrings/gitlab-ee.asc
    mode: '0644'
    force: true

- name: Apt | Add gitlab-ee repo
  ansible.builtin.deb822_repository:
    name: gitlab-ee
    install_python_debian: true
    types:
    - deb
    - deb-src
    uris: "https://packages.gitlab.com/gitlab/gitlab-ee/{{ ansible_facts[\"distribution\"] | lower }}/"
    suites: "{{ ansible_facts[\"distribution_release\"] | lower }}"
    components:
    - main
    signed_by: /etc/apt/keyrings/gitlab-ee.asc
    state: present

- name: Apt | Refresh cache after gitlab-ee repo change
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 0
  changed_when: false

- name: Apt | Set pin facts
  ansible.builtin.set_fact:
    _pin_package: gitlab-ee
    _pin_version: "{{ gitlab_release }}"
  when: gitlab_release is defined and gitlab_release != 'latest'

- name: Apt | Set gitlab-ee preferences file
  ansible.builtin.template:
    src: pin.pref.j2
    dest: "/etc/apt/preferences.d/gitlab-ee.pref"
    owner: root
    group: root
    mode: "0644"
  when: gitlab_release is defined and gitlab_release != 'latest'

- name: Apt | Install gitlab-ee
  ansible.builtin.apt:
    name: "{{ packages }}"
    state: present
    dpkg_options: 'force-confnew,force-confdef'
    autoclean: true
    autoremove: true
    update_cache: true
    cache_valid_time: 3600
  environment:
    DEBIAN_FRONTEND: noninteractive
  vars:
    packages:
    - gitlab-ee
  notify: Gitlab reconfigure

...
